spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sammy
    username: postgress
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect